{
  "name": "thaya",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf dist && mv frontend/dist dist",
    "dev:worker": "wrangler dev",
    "deploy:worker": "wrangler deploy",
    "deploy:frontend": "cd frontend && npm run build && npx wrangler pages deploy dist --project-name=thaya",
    "deploy:all": "npm run deploy:worker && npm run deploy:frontend",
    "db:migrate": "wrangler d1 execute thaya-db --file=migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute thaya-db --remote --file=migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^3.78.0",
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.5.0"
  }
}
